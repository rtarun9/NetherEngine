#pragma once

namespace nether::graphics
{
	// Both values are the same, but name is different as the names feel 'suited' in different scenarios.
	static const uint32_t FRAMES_IN_FLIGHT = 3u;
	static const uint32_t NUMBER_OF_BACK_BUFFERS = 3u;
}